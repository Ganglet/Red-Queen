# Core ML
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.40.0,<5.0.0
numpy>=1.24.0

# Phase 2 — Attack Surface Profiler
captum>=0.7.0

# Phase 3 — Attack Engine (no extra deps, uses torch autograd)

# Phase 4 — Failure Mode Clustering
umap-learn>=0.5.5
hdbscan>=0.8.33
scikit-learn>=1.3.0

# Phase 5 — LLM Agent
langgraph>=0.1.0
langchain>=0.2.0
langchain-groq>=0.1.0
llama-index>=0.10.0
llama-index-embeddings-ollama>=0.1.0

# Phase 7 — Report Generation
reportlab>=4.0.0
jinja2>=3.1.0
matplotlib>=3.7.0
Pillow>=10.0.0

# API server (Phase 8)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Testing
pytest>=8.0.0
